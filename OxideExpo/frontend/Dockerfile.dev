FROM node:20-slim

WORKDIR /app

# Copy package files first for caching
COPY package*.json ./

# Install dependencies
RUN npm ci

# The actual source will be mounted as volume
EXPOSE 3000

CMD ["npm", "run", "dev"]
